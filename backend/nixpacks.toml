[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.build]
cmds = [
  "npm install --include=dev",
  "npx prisma generate"
]

[start]
cmd = "npm run build && npx prisma db push --accept-data-loss && node dist/main"
